const User = require('../models/user')
const Admin = require('../models/admin')

const userLogin = async (req, res) => {
    try {
        
    } catch (error) {
        
    }
}



module.exports = {
    userLogin
}